Main Display Control
*** Purpose is to handle communications between the avalonia app and the renderings
- Render(AvaloniaControl control, Tile[] tiles, Renderer renderer) 
- NotifyTileHover(func<int,int,Task> handler) <-- need to control.InvalidateVisual after execution
- NotifyTileClick(func<int,int,Task> handler) <-- need to control.InvalidateVisual after execution
- NotifyTileRightClick(func<int,int,Task> handler) <-- need to control.InvalidateVisual after execution

Class Tile
*** Purpose is to hold the information necessary to tell the renderer what to render
- int x
- int y
- int elevation
- RenderComponent[] components



Class Renderer
*** Purpose is to do the actual rendering
//This needs to be re-worked, pass in the canvas and x,y and draw it
Task RenderTile(SkCanvas canvas, Tile)
Task RenderComponent(SkCanvas canvas, RenderComponent component)

Class RenderComponent
*** Purpose is to define what goes on each rendering layer
guid type
string[] params